---
phase: 02-landing-page
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User sees a hero section with impactful photography that fills the viewport on landing"
    - "User sees a featured work section highlighting curated images from across service categories"
    - "User sees elegant loading states (placeholder, blur-up, or skeleton) while images load rather than layout shift or blank space"
  artifacts: []
  key_links: []
---

<objective>
Visually verify all Phase 2 success criteria on the built landing page across desktop and mobile viewports, in both dark and light modes.

Purpose: Confirm the landing page meets user-facing quality requirements before marking Phase 2 complete.
Output: Verified landing page ready for real content in future phases.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-landing-page/02-CONTEXT.md
@.planning/phases/02-landing-page/02-01-SUMMARY.md
@.planning/phases/02-landing-page/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated pre-checks</name>
  <files></files>
  <action>
    Run automated verification before human review:

    1. `npm run build` -- must succeed with zero errors
    2. Count generated pages -- should include index.html
    3. Check build output for optimized image variants in dist/_astro/
    4. Verify hero image preload link exists in built index.html:
       ```bash
       grep -c 'rel="preload".*as="image"' dist/index.html
       ```
    5. Start preview server for human verification:
       ```bash
       npm run preview
       ```
    6. Report all results to user before checkpoint
  </action>
  <verify>
    - Build succeeds
    - index.html exists in dist/
    - Image variants generated in dist/_astro/
    - Preload link present in index.html head
  </verify>
  <done>Build passes all automated checks and preview server is running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete landing page with:
    - Full-viewport hero carousel (4 images, auto-rotating, navigation arrows + dots)
    - Name/tagline overlay ("Simon Wickes Photography") at bottom-left
    - Pause/play button for carousel at bottom-right
    - Intro text section after hero
    - Featured work masonry grid (12 images across 8 categories)
    - Category labels on hover for each featured image
    - Dual CTAs: "Explore Services" + "Get in Touch"
    - Dark solid placeholder while hero loads
    - Skeleton loading states in featured grid
  </what-built>
  <how-to-verify>
    Open http://localhost:4321/ in your browser and verify:

    **Hero Section:**
    1. Hero fills the entire viewport height on load
    2. Images auto-rotate every ~4.5 seconds with slide transition
    3. Navigation arrows (left/right) are visible and functional
    4. Pagination dots are visible at bottom and clickable
    5. "Simon Wickes Photography" + tagline visible at bottom-left
    6. Pause/play button visible at bottom-right and works

    **Featured Work:**
    7. Masonry grid displays below the hero section
    8. Grid shows ~12 images in a Pinterest-style flowing layout
    9. Hovering over any image reveals category name label
    10. Clicking a featured image navigates to the correct service page

    **CTAs:**
    11. Two CTA buttons appear at end of featured section
    12. "Explore Services" button links to a service page
    13. "Get in Touch" button links to /contact

    **Responsive (resize browser or use DevTools):**
    14. At mobile width (~375px): grid shows 1 column, hero fills viewport
    15. At tablet (~768px): grid shows 2 columns
    16. At desktop (~1200px): grid shows 3-4 columns

    **Theme:**
    17. Toggle dark/light mode -- all sections render correctly in both themes
    18. Hero placeholder background is dark in both modes

    **Accessibility:**
    19. Enable "Reduce Motion" in OS settings -- carousel should stop auto-rotating
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 2, or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
All three Phase 2 success criteria verified:
1. Hero section with impactful photography fills viewport
2. Featured work section highlights curated images across categories
3. Elegant loading states (dark placeholder + skeleton cards) prevent blank space
</verification>

<success_criteria>
- Human confirms all verification items pass
- Landing page is visually striking and functional
- Phase 2 can be marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page/02-03-SUMMARY.md`
</output>
